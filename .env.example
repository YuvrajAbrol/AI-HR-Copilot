# Copy to `.env` and fill in when wiring the real Azure backend.
# The mock UI works without any of these.

# Base URL of your Azure App Service API
VITE_API_BASE_URL=https://your-app.azurewebsites.net

# (Server-side only — do NOT expose these in the frontend build)
# AZURE_OPENAI_ENDPOINT=
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_DEPLOYMENT=
# AZURE_SEARCH_ENDPOINT=
# AZURE_SEARCH_API_KEY=
# AZURE_SEARCH_INDEX=
# AZURE_SQL_CONNECTION_STRING=
